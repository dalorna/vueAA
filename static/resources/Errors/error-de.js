export const errorde = {
  de: {
    messages: {
      ext: () => 'Nur PDF, DOC und DOCX erlaubt',
      min: (name, ref) => 'Mindestanzahl von Zeichen für ' + name + ' ist' + ref[0]
    }
  }
};

export default errorde;
